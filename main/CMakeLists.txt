# Edit following two lines to set component requirements (see docs)
set(COMPONENT_REQUIRES )
set(COMPONENT_PRIV_REQUIRES )

set(COMPONENT_SRCS "server.c" "main.c" "task_manager.c")
set(COMPONENT_ADD_INCLUDEDIRS "include")

register_component()
